[project]
name = "python-project-49"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kir", email = "advtrail@yandex.ru" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
brain-progression = "brain_progression.scripts.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["brain_progression"]

[dependency-groups]
dev = [
    "ruff>=0.16.2",
]
